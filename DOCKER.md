Python 3.11-slim: ~240mb
Python 3.11-alpine: ~160mb

I belive they differ because the alpine version of Python strips out more unecessary things than the slim version of Python, resulting in the lower container size that the alpine version has over the slim version
